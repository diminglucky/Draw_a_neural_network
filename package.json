{
  "name": "synapse-studio",
  "version": "0.3.0",
  "description": "LLM-driven neural network architecture compiler with native Visio rendering",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-builder --win",
    "dist": "electron-builder",
    "test": "node --import ./test-setup.mjs --test"
  },
  "build": {
    "appId": "com.synapse.studio",
    "productName": "Synapse Studio",
    "files": [
      "**/*",
      "!**/*.test.mjs",
      "!tmp/**",
      "!artifacts/**",
      "!docs/**",
      "!llm-config.json",
      "!*.md"
    ],
    "extraResources": [
      { "from": "visio-bridge.ps1", "to": "visio-bridge.ps1" },
      { "from": "visio-create-empty.ps1", "to": "visio-create-empty.ps1" }
    ],
    "win": {
      "target": [
        "nsis"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "devDependencies": {
    "electron": "^44.2.0",
    "electron-builder": "^26.15.3"
  }
}
