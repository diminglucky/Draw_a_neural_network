# Agent Instructions

在任何修改前，未来编码 Agent 必须先阅读 [docs/START_HERE.md](docs/START_HERE.md) 和 [canonical design](docs/superpowers/specs/2026-08-14-universal-neural-figure-compiler-design.md)。

当前产品分支是 `agent`，且当前优先级从 P0 开始。不得将五个专用 grammar 宣称为任意网络的通用支持，也不得把 mock、协议或单元测试当作真实 Windows/Visio 的创建、保存、重开与 readback 验收。

修改前先检查工作树状态；保留不属于当前任务的改动和其他工作树。只有前置门完成并有相应证据时，才能推进到 P1、P2、P3 或 P4。
