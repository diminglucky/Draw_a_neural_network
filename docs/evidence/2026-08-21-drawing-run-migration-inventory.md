# DrawingRun Migration Inventory — 2026-08-21

**Status:** Phase 0 repository inventory and governance evidence. It records source facts and migration decisions; it is not M2.12, PVP, Worker, Visio, or real-host acceptance evidence.

## Classification key

- **Public** means an allowlisted response or validated route payload.
- **Private** means source bytes, provider context, credentials, filesystem/path details, worker internals, or stored execution detail.
- **Mixed** means an entrypoint has a public projection but must retain private inputs or internals behind it.

| Current entrypoint | Incoming DTO or trigger | Emitted artifact | Authority and data classification | Direct tests | DrawingRun successor | Default selection for a new request | Retirement or fence condition | Rollback behavior |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `routes.ts` Agent chat + `agent-service.ts` / `agent-runtime.ts` | Authenticated chat, attachment, and action DTOs | Legacy agent response and draft actions | Legacy chat adapter; mixed | `agent-service.test.ts`, `agent-runtime.test.ts`, `agent-actions.test.ts` | `DrawingIntent` adapter | Compatibility read/response only; new versioned drawing requests select Coordinator, never this route as a structural authority. | No new structural or native authority; remove new-route use only after Coordinator route coverage. | Disable versioned route selection and return a controlled unavailable/legacy-compatible response; do not forward the new request here. |
| `network-ir.ts`, `network-ir-v1-adapter.ts`, `network-ir-v2-to-v3.ts` | Legacy NetworkIR v1/v2/v3 values | Canonical legacy NetworkIR | Legacy graph authority for saved drafts; mixed | `network-ir*.test.ts` | UGS adapter | Read existing saved drafts through an adapter; all new topology enters `DrawingIntent` and canonical UGS. | Retain read compatibility; stop new canonical graph writes after migration tests cover saved drafts. | Disable the adapter for new requests while preserving the original saved-draft reader; never reinterpret a new request as NetworkIR. |
| `/api/figure-analyses` + `figure-analysis-service.ts` | Authenticated analysis source request | `FigureAnalysisRecord` and public analysis projection | Legacy analysis/session authority; mixed | `figure-analysis-service.test.ts`, `figure-analysis-routes.test.ts` | Receipt -> EvidencePack | Legacy analysis remains explicit compatibility; new DrawingRun intake creates a private receipt. | Fence to legacy route after versioned DrawingRun route preserves public behavior. | Disable new route selection and report the versioned capability unavailable; no raw source is re-routed into legacy free-text analysis. |
| `figure-analysis-preview-service.ts` | Stored analysis record | Preview response | Legacy preview projection; public projection over private record | `figure-analysis-preview-service.test.ts`, `figure-analysis-preview-routes.test.ts` | Public DrawingRun projection | Serve existing records only; new runs project their own safe status/preview. | Retire only when DrawingRun returns equivalent safe preview/clarification states. | Keep existing-record reads; disable new-run projection rather than compiling a new request through this service. |
| `figure-draft-service.ts` / `figure-draft-preview-service.ts` | Draft, revision, confirmation | Owner-scoped draft revision and preview | Existing session/revision authority; mixed | `figure-draft*.test.ts` | Session adapter over DrawingRun | Preserve existing drafts and revisions; new DrawingRun revisions become the default after cutover. | Preserve saved drafts; no new raw-input compilation after Phase 5 cutover. | Stop new DrawingRun selection and retain explicit draft reads only; never create a second raw-input compilation. |
| `prompt-universal-graph-spec.ts` | Typed prompt declaration | UGS | Deterministic structural adapter; mixed | `prompt-universal-graph-spec.test.ts` | Typed-declaration receipt adapter | Continue as a deterministic fact source behind receipt intake, not a run/PVP creator. | Keep as analyzer; it cannot mint a separate run or PVP. | Disable the new receipt adapter and return controlled intake unavailability; do not call it as an alternate PVP path. |
| `static-pytorch-source-analyzer.ts` + `static-pytorch-universal-graph-spec.ts` | Source ID, hash, and non-executed PyTorch text | Analysis facts and UGS | Deterministic non-executing analyzer; private source, public safe graph | `static-pytorch-source-analyzer.test.ts`, `static-pytorch-universal-graph-spec.test.ts` | Static receipt/EvidencePack adapter | Continue as verified static facts behind a private receipt; no direct new graph or native output. | Keep as analyzer; no source execution and no direct native path. | Disable receipt-to-fact selection and return a controlled unsupported result; never execute or send source to legacy native work. |
| `universal-graph-spec.ts` + adapter | UGS or ArchitectureIRv3 | Validated canonical UGS and eligibility | Current canonical structural contract; public-safe projection only | `universal-graph-spec.test.ts`, `universal-graph-spec-adapter.test.ts` | Formal UGS | Default canonical structural truth for formal results; compatibility adapters may feed it. | Freeze as sole graph truth; compatibility adapters may feed it but never bypass it. | No fallback is applicable: reject a new run that cannot produce formal UGS or return clarification/rejection. |
| `universal-input-compilation-service.ts` | Typed prompt, static source, or architecture-description compatibility input | UGS plus publication preview/clarification | Transitional compiler; mixed | `universal-input-compilation-service.test.ts` | Coordinator + StructuralAssessment | Existing compatibility callers only; new requests use Coordinator once enabled. | No new route may call the M2.12 compatibility interpreter after cutover. | Disable Coordinator route selection and return controlled unavailability; do not call this compiler for the new request. |
| `evidence-augmented-ugs-interpreter.ts` / harness | `BoundedInterpretationRequest` and provider-local IDs | Candidate/formal compatibility UGS | Regression-only prototype; private provider text and unsafe historical identifiers | `evidence-augmented-ugs-interpreter.test.ts` | Receipt-bound Structural Harness | Regression tests only; it is never a default or fallback for new requests. | Retire after all callers use local proposals, canonical evidence, and `StructuralAssessment`; preserve negative regressions. | Keep negative regression fixtures; reject/disable the new request rather than invoking this interpreter. |
| `evidence-constrained-drawing-session.ts` | Owner/device-bound accepted adapter input and answer | UGS/GPG/PVP session or one clarification | Current session/confirmation authority; mixed | `evidence-constrained-drawing-session.test.ts` | Presentation adapter over DrawingRun | Existing session compatibility only; new revision control moves to DrawingRun after Phase 5. | Cannot compile a second graph from raw input after Phase 5. | Disable new session selection and keep old session reads; no raw input is recompiled through the legacy session. |
| `general-publication-graph.ts` + `general-publication-figure-plan.ts` | Formal UGS | GPG and general figure plan | Deterministic visual composition primitive; public-safe semantic artifact | `general-publication-graph.test.ts`, `general-publication-figure-plan.test.ts` | Formal UGS -> GPG | Continue as the default formal composition primitive after a formal UGS only. | Keep as composition primitive; never accept candidate topology. | Reject/clarify without a formal UGS; never fall back to a template or candidate topology. |
| `publication-visual-plan-compiler.ts`, QA, and preview service | UGS, GPG, update identity | PVP, QA result, browser preview | Current visual truth; mixed identity metadata with allowlisted public preview | `publication-visual-plan-*.test.ts`, `publication-visual-preview-service.test.ts` | Formal PVP stage | Default composer only after formal UGS/GPG and QA; not callable from candidate or raw input. | Freeze PVP as only visual input to sealing; semantic grammar work belongs to M2.13. | Disable PVP stage and return pre-composition status; never substitute legacy preview geometry or a model template. |
| `/api/universal-figure-previews` and draft publication-preview route | Authenticated legacy universal input or stored draft revision | Public PVP preview/clarification | Legacy HTTP preview boundary; public projection | `universal-pvp-preview-*.test.ts`, route tests | Versioned DrawingRun preview route | Existing request compatibility or stored-draft read only; new route is Coordinator-owned. | Keep response compatibility; do not accept page target, native control, or raw provider data. | Disable versioned route and return controlled unavailable/legacy-compatible response without forwarding new input to this route. |
| `analysis-plan-snapshot*`, `plan-snapshot*`, and `generic-plan-snapshot*` | Owner/device/revision-bound plan or PVP | Immutable snapshot | Snapshot/persistence boundary; mixed | `analysis-plan-snapshot*.test.ts`, `plan-snapshot*.test.ts`, `generic-plan-snapshot*.test.ts` | Coordinator ArtifactStore | Existing verified PVP snapshots remain readable; new snapshot creation follows Coordinator-owned formal lineage. | Continue only through verified formal PVP lineage; snapshots are not page bindings. | Disable new snapshot creation and preserve existing reads; never fabricate a snapshot from raw/candidate input. |
| `agent-visio-bridge.ts` + execution snapshot | VGG16-shaped legacy NetworkIR draft/revision | VGG-specific diagram and execution snapshot | Regression fixture authority; mixed | `agent-visio-bridge.test.ts`, `agent-visio-execution-snapshot.test.ts`, `agent-vgg16-visio-acceptance.test.ts` | None; fixture only | Fixture/regression invocation only; no generic or new request selects it. | Never select a generic grammar or current-page path; remove only after fixture regressions move to explicit legacy coverage. | Preserve fixture tests; reject the new request instead of selecting a VGG bridge. |
| Export routes + `figure-export-service.ts` / universal runners | Confirmed stored snapshot, owner/device, job | Sealed export job and worker result | Historical export authority; mixed | `figure-export-service.test.ts`, `universal-figure-export-*.test.ts` | Sealed execution request | Existing explicitly requested export only; new current-page work must wait for M3.6. | Cannot claim current-page update; retain only behind explicit export compatibility until M3.6. | Disable new execution selection and return controlled unavailable status; never convert a new page update into export. |
| `visio-protocol.ts`, `visio-worker-client.ts`, `visio-job-runner.ts` | Legacy diagram request and job | Worker response and `VisioReadback` | Legacy Worker boundary; private process/path details, public result projection | `visio-protocol.test.ts`, `visio-worker-client.test.ts`, `visio-job-runner.test.ts`, `visio-readback.test.ts` | Restricted Worker after M3.3 | Existing job compatibility only; no new DrawingRun calls it before sealed binding. | Fence from raw input and page selection; no DrawingRun path uses it before sealed binding. | Disable new Worker dispatch and preserve existing job reads; never send raw input or selected-page intent here. |
| `visio-session-protocol.ts` + long-lived client/session | Trusted tenant/user/device/workflow command | Session response with optional readback | Existing session authority; mixed | `visio-universal-worker-client.test.ts`, Worker session tests | Page-binding service and sealed Worker request | Existing trusted session compatibility only; new page updates require explicit binding. | Retire direct callers after selected-page binding and cancellation/readback contracts exist. | Disable new binding selection and return a controlled no-update result; never attach a new run to the long-lived session. |
| `visio-universal-protocol.ts` + `visio-universal-worker-client.ts` | Sealed plan envelope and binding | Universal Worker response and native readback | Sealed generic execution boundary; private signing material, allowlisted result | `visio-universal-protocol.test.ts`, `visio-universal-worker-client.test.ts` | M3.2-M3.4 execution chain | Preserve M3.1 Snapshot-only behavior; selected-page execution stays unavailable until every M3.6 gate. | Preserve M3.1 boundary; it is not current-page authority without M3.6 gates. | Disable selected-page capability selection; do not broaden M3.1 into Worker/page authority. |
| `workers/visio-worker` document lifecycle and COM session operations | Worker protocol and lifecycle commands | Native shapes, session status, and readback | Native implementation boundary; private COM/path/process state | `VisioComSessionOperationsTests.cs`, `VisioSessionManagerTests.cs`, `VisioComSessionLiveAcceptanceTests.cs` | M3.3-M3.6 Worker/readback | Existing lifecycle remains isolated; no new request can select document/page creation or COM directly. | Existing `OpenOrCreate` remains isolated; current-page work requires discovery, explicit binding, owned region, conflict check, and real host. | Disable page-binding/Worker dispatch; never call `OpenOrCreate` or create a document/page as a new-request fallback. |

## Migration decisions

1. UGS remains the only structural truth and PVP remains the only visual truth. NetworkIR, FigureAnalysis, and the free-text M2.12 prototype are adapters or fixtures, not another future pipeline.
2. New requests begin at `DrawingIntent`; they cannot route raw input to legacy preview, export, or Visio paths. Candidate or blocking assessments stop before PVP, Snapshot, export, Worker, COM, or page binding.
3. M3.6 is the only planned current-page Visio claim. It needs a selected existing target, an Agent-owned region, owner/device/run/revision-bound sealing, preserved-shape conflict checks, independent readback, and an explicit real-host lifecycle matrix.
4. Rollback disables a new versioned route or binding selection. It returns a controlled unavailable or explicit compatibility response and never silently reroutes a new request through the free-text interpreter, legacy preview/export route, `OpenOrCreate`, or a VGG-specific bridge. Legacy artifacts remain read-compatible only where their row explicitly permits it.
